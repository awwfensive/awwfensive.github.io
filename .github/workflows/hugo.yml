name: Deploy Hugo site

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.138.0'
          extended: true

      - name: Build
        run: |
          rm -rf public/ resources/ .hugo_build.lock
          hugo --minify --cleanDestinationDir --baseURL "https://awwfensive.site/"
          
      - name: Check for localhost in links
        run: |
          if grep -r 'href=".*localhost' public/ || grep -r "href='.*localhost" public/; then
            echo "Error: localhost found in href attributes"
            exit 1
          else
            echo "No localhost references in links - build is clean"
          fi

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          cname: awwfensive.site
          force_orphan: true